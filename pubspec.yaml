name: ad_creator_manage_main

description: Offline campaign management demo with Hive storage.

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  syncfusion_flutter_charts: ^23.1.36

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^2.0.0
